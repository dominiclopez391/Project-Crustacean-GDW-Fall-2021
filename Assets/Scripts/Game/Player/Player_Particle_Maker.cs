using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player_Particle_Maker : MonoBehaviour
{
    public GameObject jumpPrefab, wallClingPrefab, landPrefab, wallJumpPrefab;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
